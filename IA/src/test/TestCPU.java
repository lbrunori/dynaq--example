package test;

import org.junit.Test;
import source.CPU;
import source.EntornoReal.ProcesoTipoUno;
import static org.junit.Assert.assertEquals;
/**
 * Created by lbrunori on 09/02/17.
 */
public class TestCPU {

    @Test
    public void testCPU(){
        /*ProcesoTipoUno ptu = new ProcesoTipoUno();
        CPU cpu = CPU.getInstancia();
        if(cpu.getEstaDisponible()){
            cpu.asignarNuevoProceso();
        }
        while(!cpu.getEstaDisponible()){
            cpu.procesar();
        }

        ProcesoTipoUno ptu1 = new ProcesoTipoUno();
        if(cpu.getEstaDisponible()){
            cpu.asignarNuevoProceso();
        }
        while(!cpu.getEstaDisponible()){
            cpu.procesar();
        }

        assertEquals(true , cpu.getEstaDisponible());*/

    }
}
